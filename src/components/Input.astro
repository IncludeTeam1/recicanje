---
const props = Astro.props;
---

<input
  class=`p-2 outline-none rounded-lg text-black  ${props.class}`
  type={props.type}
  placeholder={props.placeholder}
  id={props.id}
  name={props.name}
/>
