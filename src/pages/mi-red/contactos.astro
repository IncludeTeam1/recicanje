---
import LayoutPrincipal from '../../layouts/LayoutPrincipal.astro';
export const prerender = false;
import { SeccionContactosConectados } from '../../components/react/red/SeccionContactosConectados';
import { BASE_URL_API } from '../../config';
---

<LayoutPrincipal title="Mis contactos">
  <main
    id="page-mi-red"
    class="w-full py-10 px-5 seccion-observar bg-white gap-2 flex flex-col max-w-5xl mx-auto"
  >
    <div class="flex justify-between">
      <h2
        class="text-2xl md:text-3xl font-semibold
      bg-gradient-to-r from-teal-400 to-teal-800 bg-clip-text text-transparent"
      >
        Contactos
      </h2>
      <a href={`${BASE_URL_API}/mi-red/contactos`} class="text-xl">🔙</a>
    </div>
    <SeccionContactosConectados client:only />
  </main>
</LayoutPrincipal>
